{% extends 'base.html.twig' %}

{% block title %}Modifier mot de passe{% endblock %}

{% block body %}
<div class="mainContainer">

    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header text-center mt-5">Modification de mot de passe</h1>
            </div>
        </div>

        <div class="m-5">
            <div class="container-fluid h-100">
                <div class="row justify-content-center align-items-center mt-3">
                    <div class="col col-sm-6 col-lg-4 mt-5">
                        <div class="row justify-content-between">
                            {{ include('structure_new_password/_form.html.twig') }}
                        </div>
                        <a href="{{ path('app_structure_consult') }}" class="btn btn-outline-success mt-2">Retour</a>
                    </div>
                </div>
            </div>
        </div>

    </div>

</div>
{% endblock %}
